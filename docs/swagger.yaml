basePath: /gestrym-progress
info:
  contact: {}
  description: API para el manejo de progresos.
  title: Gestrym API Progress
  version: "1.0"
paths: {}
securityDefinitions:
  ApiKeyAuth:
    in: header
    name: X-API-Key
    type: apiKey
  BasicAuth:
    type: basic
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
